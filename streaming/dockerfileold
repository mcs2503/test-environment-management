FROM python:3.9-slim

WORKDIR /app

COPY video_stream.py /app
COPY video.mp4 /app

RUN apt-get update && apt-get install -y \
    ffmpeg \
    && rm -rf /var/lib/apt/lists/*

CMD ["python", "video_stream.py"]
